// Loaded first by the tests: contact details are required settings, so tests use the
// fictional demo brand's (only if real ones aren't already set).
process.env.CONTACT_EMAIL ??= "team@ridgeline-shields.example";
process.env.CONTACT_PHONE ??= "555-010-0199";
