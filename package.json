{
  "name": "evolution-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Retrieval-augmented Q&A assistant for Evolution Windshields \u2014 answers only from approved documents, with citations.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "prepare": "git config core.hooksPath .githooks || true",
    "check:keys": "tsx --env-file-if-exists=.env.local scripts/check-keys.ts",
    "db:migrate": "tsx --env-file-if-exists=.env.local scripts/migrate.ts",
    "ingest": "tsx --env-file-if-exists=.env.local scripts/ingest.ts",
    "ask": "tsx --env-file-if-exists=.env.local scripts/ask.ts",
    "eval": "tsx --env-file-if-exists=.env.local scripts/eval.ts",
    "check:private": "node scripts/check-staged.mjs --all",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "engines": {
    "node": ">=20.6"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@supabase/supabase-js": "^2.117.1",
    "csv-parse": "^7.0.2",
    "next": "^16.3.6",
    "postgres": "^3.4.9",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "unpdf": "^1.8.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "tsx": "^4.23.15",
    "typescript": "^6.0.3"
  }
}
